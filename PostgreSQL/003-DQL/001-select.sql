/*
    Select
    - Some sources on the internet classify SELECT as DML but 
      it might make more sense as a DQL.
    - Pending: DML or DQL?
*/

-- Basic select
SELECT title, year_published from movies;